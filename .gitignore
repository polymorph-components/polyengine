/target/
# @polyengine/translator packaged asset (copied by `just shim`)
translator/translator_shim.wasm
harness/generated/
harness/generated-wasmtime/
examples/guests/build/
node_modules/
dist/
# M3 browser lane: playwright browser downloads (PLAYWRIGHT_BROWSERS_PATH)
.browser-cache/
# Engine-shell canary lanes (issue #22): fetched SpiderMonkey nightly / JSC
# trunk / Deno canary binaries.
.shell-cache/
*.tmp
# ports/webcrypto conformance runner output (conformance/run.ts)
ports/webcrypto/conformance/results-*.jsonl

# bench/boundary: local artifacts (the recipe rebuilds them)
bench/boundary/polyengine-embedder.local.mjs
bench/boundary/generated/
bench/boundary/node_modules/
bench/boundary/guest/target/
# @polyengine/translator packaged asset (copied by `just shim`)
translator/translator_shim.wasm

# The npm distribution (tools/npm-build/build.ts): built artifacts and the
# translator's staged source tree, both reproducible from a checkout.
/npm/
translator/.npm-stage/
